<p style="color: green"><%= notice %></p>

<%= render @track %>

<div class="mt-4 d-flex justify-content-between">
  <%= link_to 'Edit Track', edit_track_path(@track), class: "btn btn-warning" %>
  <%= link_to 'Delete Track', @track, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
</div>
